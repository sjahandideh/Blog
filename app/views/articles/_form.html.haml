= simple_form_for(@article) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :content
    = f.input :author_id, collection: User.all, label_method: :name, value_method: :id, prompt: "Select Author"

  .form-actions
    = f.button :submit
